#include <iostream>

class Render
{
    public:
        Scene scene;
        GamseObjects gameObjects;

    Render (scene, gamseObjects) {
        Scene = Scene;
        GamseObjects = gamseObjects;
    }
}